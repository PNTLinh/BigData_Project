apiVersion: 1

providers:
  - name: "MTA Dashboards"
    orgId: 1
    folder: "MTA Monitoring"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
